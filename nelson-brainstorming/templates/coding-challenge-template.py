"""
Plataforma — Nombre del Challenge (Dificultad: Easy/Medium/Hard)
Fecha: YYYY-MM-DD

Enunciado:
    [Copiar enunciado exacto de la imagen/plataforma]

Ejemplos válidos:   "..." → resultado
Ejemplos inválidos: "..." → 0 o False (razón)

Casos de prueba:
    "..."  → N  (descripción)
    "..."  → N  (descripción)
    ""     → 0  (string vacío)
"""
from typing import Any


def CodingChallenge(strParam: str) -> Any:
    """
    [Una línea describiendo qué hace la función.]

    Args:
        strParam: [Descripción del parámetro de entrada.]

    Returns:
        [Descripción del valor de retorno.]
    """
    # CONSTANTES arriba, en UPPER_CASE, con tipos inmutables (frozenset, tuple)
    # CONSTANT: frozenset[str] = frozenset("abc")

    result: Any = None  # type hint en variables locales

    # Lógica principal — comentarios en inglés sobre decisiones no obvias

    return result


# keep this function call here
print(CodingChallenge(input()))
